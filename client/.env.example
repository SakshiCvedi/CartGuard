# Only needed when the frontend is deployed separately from the backend
# (e.g. frontend on Vercel, backend on Railway/Render). Leave unset for
# local dev - Vite's proxy in vite.config.js handles /api locally.
VITE_API_URL="https://cartguard-r4ew.onrender.com/api"